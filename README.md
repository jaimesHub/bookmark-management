# Bookmark Management API

A RESTful API service built with Go, following Clean Architecture principles.

## Tech Stack

- **Go** 1.26.2
- **HTTP Framework**: [Gin](https://github.com/gin-gonic/gin)
- **Config**: [envconfig](https://github.com/kelseyhightower/envconfig) — all config via environment variables
- **Redis Client**: [go-redis/v9](https://github.com/redis/go-redis)
- **Redis Mock (tests)**: [miniredis/v2](https://github.com/alicebob/miniredis)
- **Testing**: [testify](https://github.com/stretchr/testify) + [mockery v2](https://github.com/vektra/mockery)
- **API Docs**: [Swagger/OpenAPI](https://github.com/swaggo/swag) via `swag`

## Project Structure

```
bookmark-management/
├── cmd/
│   └── api/
│       └── main.go                      # Entrypoint — wires all dependencies
├── docs/                                # Auto-generated Swagger docs (do not edit manually)
│   ├── docs.go
│   ├── swagger.json
│   └── swagger.yaml
├── internal/
│   ├── api/
│   │   ├── api.go                       # Gin engine, route registration
│   │   └── config.go                    # API-level config (APP_PORT, SWAGGER_ENABLED)
│   ├── handler/
│   │   ├── healthcheck.go               # HTTP handler — GET /health-check
│   │   ├── healthcheck_test.go
│   │   ├── shorten.go                   # HTTP handler — POST /v1/links/shorten
│   │   └── shorten_test.go
│   ├── integration_test/
│   │   ├── checkhealth_ep_test.go       # End-to-end test — health check
│   │   └── shorten_ep_test.go           # End-to-end test — shorten endpoint
│   ├── repository/
│   │   ├── ping.go                      # Redis liveness check — implements service.Pinger
│   │   ├── ping_test.go
│   │   ├── urlstorage.go                # Redis implementation — StoreURL / GetURL / StoreURLIfNotExists
│   │   └── urlstorage_test.go
│   ├── service/
│   │   ├── config.go                    # Service-level config (SERVICE_NAME, INSTANCE_ID)
│   │   ├── healthcheck.go
│   │   ├── healthcheck_test.go
│   │   ├── shorten.go                   # URLStorage interface + ShortenService
│   │   ├── shorten_test.go
│   │   └── mocks/
│   │       ├── healthcheck.go           # Generated by mockery v2
│   │       ├── pinger.go
│   │       ├── shorten_service.go
│   │       └── urlstorage.go
│   └── testutil/
│       └── redis.go                     # InitMockRedis + InitClosedRedis — miniredis helpers for tests
├── pkg/
│   ├── redis/
│   │   ├── client.go                    # Redis client factory
│   │   └── config.go                    # Redis config via envconfig
│   └── stringutils/
│       └── code.go                      # GenerateCode — crypto/rand 7-char alphanumeric
├── Makefile
├── go.mod
└── go.sum
```

## Configuration

All configuration is read from environment variables at startup — no config files.

| Variable           | Default              | Description                                              |
|--------------------|----------------------|----------------------------------------------------------|
| `APP_PORT`         | `8080`               | Port the HTTP server listens on                          |
| `SWAGGER_ENABLED`  | `false`              | Set to `true` to expose `/swagger/*` UI                  |
| `SERVICE_NAME`     | `bookmark_service`   | Service identifier returned by `/health-check`           |
| `INSTANCE_ID`      | *(empty)*            | Instance identifier; auto-generated UUID if not provided |
| `REDIS_ADDR`       | `localhost:6379`     | Redis server address                                     |
| `REDIS_PASSWORD`   | *(empty)*            | Redis password (leave empty for local)                   |
| `REDIS_DB`         | `0`                  | Redis database index (0–15)                              |

## Getting Started

### Prerequisites

- Go 1.26.2 (managed via `asdf` — `asdf local golang 1.26.2`)
- Docker (để chạy Redis locally):

```bash
docker run --name redis -d -p 6379:6379 redis:alpine
```

- Development tools (install once):

```bash
make install-tools
```

This installs `swag`, `mockery v2`, and `goimports`.

### Run

```bash
make run
```

Generates Swagger docs, then starts the server on `localhost:8080`.

With Swagger UI enabled:

```bash
SWAGGER_ENABLED=true make run
# → http://localhost:8080/swagger/index.html
```

### Build

```bash
make build
# → bin/api
```

## API Endpoints

### `GET /health-check`

Returns service health status.

**Response `200 OK`:**

```json
{
  "message": "OK",
  "service_name": "bookmark_service",
  "instance_id": "a1b2c3d4-e5f6-..."
}
```

**Response `500 Internal Server Error`:**

```json
{
  "error": "<reason>"
}
```

---

### `POST /v1/links/shorten`

Generates a 7-character alphanumeric short code for the given URL and stores it in Redis with the specified TTL.

**Request body:**

```json
{
  "url": "https://example.com",
  "exp": 3600
}
```

| Field | Type   | Required | Description               |
|-------|--------|----------|---------------------------|
| `url` | string | ✅        | Must be a valid URL        |
| `exp` | int    | ✅        | TTL in seconds (min: 1)   |

**Response `201 Created`:**

```json
{
  "code": "aB3xYz9",
  "message": "Shorten URL generated successfully!"
}
```

**Response `400 Bad Request`** *(missing/invalid fields)*:

```json
{
  "error": "invalid request"
}
```

**Response `500 Internal Server Error`:**

```json
{
  "error": "internal server error"
}
```

---

### `GET /swagger/*` *(when `SWAGGER_ENABLED=true`)*

Interactive Swagger UI.

## Testing

```bash
# Run all tests (unit + integration)
make test

# Run with coverage report → coverage.html (threshold: 80%)
make test-coverage
```

Tests follow Clean Architecture boundaries:
- Unit tests mock at layer interfaces
- Integration tests spin up the real HTTP engine with miniredis
- Coverage threshold: **80%** (excludes `mocks`, `main.go`, `test`, `pkg/redis`)

## Makefile Targets

```
make run              Run the server (regenerates Swagger docs first)
make test             Run all tests
make test-coverage    Run tests + coverage report (threshold: 80%)
make build            Build binary → bin/api
make clean            Remove bin/, coverage.tmp, coverage.out, coverage.html
make swag             Regenerate Swagger docs only
make install-tools    Install swag, mockery v2, goimports
make fmt              Format code (go fmt + goimports)
```
