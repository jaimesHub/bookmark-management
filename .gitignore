# IDE
.idea/
*.swp
*.swo
*~

# Claude Code
CLAUDE.md

# Go
vendor/
bin/

# Go test coverage files
coverage.out
coverage.tmp
coverage.html

# Local scratch / manual test files
cmd/test/
pkg/redis/mock.go

# Environment file (use .env.example as template)
.env

# RSA keypair (Lec-6 dev — generated by `make generate-rsa-key`).
# NEVER commit private keys — production keys injected via VM secret mount.
keys/
